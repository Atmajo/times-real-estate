version: '3'

services:
  realestate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: realestate
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped